define([
	"dojo/_base/declare",
	"../AbstractRegistry"
], function(declare, AbstractRegistry){
	
	var ThemeRegistry = declare(AbstractRegistry, {
		//... not extending yet
	});
	
	return new ThemeRegistry;
	
});